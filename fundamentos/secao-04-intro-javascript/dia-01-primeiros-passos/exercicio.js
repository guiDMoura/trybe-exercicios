//Operações:
    //Constantes:
    const a = 7
    const b = 8

    //Exibição das constantes:
    console.log('a = ' + (a) + ' b = ' + (b))

    //Operações:
    console.log('Adição: ' + (a + b));
    console.log('Subtração: ' + (a - b));
    console.log('Multiplicação: ' + (a * b));
    console.log('Divisão: ' + (a / b));
    console.log('Módulo: ' + (a % b));